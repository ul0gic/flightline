# Internal scaffolding — never ships with the repo
.claude/
.project/
.playwright-mcp/

# Marketing site — moves to its own repo before go-live
web/

# Build output
/bin/
/dist/
*.exe

# pnpm store (leaks to repo root when pnpm runs in web/)
.pnpm-store/

# Coverage
coverage.out
coverage.html

# Editor / OS
.DS_Store
*.swp
*.swo
.idea/
.vscode/

# Credentials — defense in depth (creds live at ~/.appstoreconnect/, not in repo)
*.p8
.env
.env.*
!.env.example
