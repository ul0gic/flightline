apiVersion: skipper.corelift.io/v1alpha1
kind: AppState
metadata:
  bundleId: com.example.passref
  version: "1.0"
spec:
  reviewerDemo:
    username: "demo@example.com"
    # Must match ^env:[A-Z_][A-Z0-9_]*$ — lowercase rejected.
    passwordRef: "env:demo_password"
